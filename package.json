{
  "name": "careporter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm --workspace @careporter/client run dev",
    "build": "npm run build:client",
    "build:client": "npm --workspace @careporter/client run build",
    "preview:client": "npm --workspace @careporter/client run preview"
  }
}
